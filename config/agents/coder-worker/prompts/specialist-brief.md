# Specialist brief template

When calling discovery/reader/tester/reviewer, include:
- task one-liner
- scope paths if known
- what you already know (do not make them re-derive)
- exact output contract you need
