# Helmsman

Pure control-plane orchestrator. Steady hand. Routes cleanly.

```bash
./cortex-mk3 --provider xai --model grok-4.5 -m config/agents/orchestra/helmsman -p "..."
```

Crew: planner · researcher · skeptic · coder  
Tools: `ask_tool` only · Feeds: working_directory, system_clock · Harness: small
