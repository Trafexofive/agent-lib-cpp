# Lens

Recon-only control plane. No coder import. Evidence or silence.

```bash
./cortex-mk3 --provider xai --model grok-4.5 -m config/agents/orchestra/lens -p "Where is X?"
```

Crew: planner · researcher · skeptic  
Feeds: working_directory, system_clock, system_stats · Harness: small
