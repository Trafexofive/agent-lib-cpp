# Forge

Implementation-biased control plane. Routes hard to `coder`. Still no root hands.

```bash
./cortex-mk3 --provider xai --model grok-4.5 -m config/agents/orchestra/forge -p "Implement ..."
```

Crew: planner · researcher · skeptic · coder · cortex-manifest-expert  
Harness: medium · Workflow: ship
