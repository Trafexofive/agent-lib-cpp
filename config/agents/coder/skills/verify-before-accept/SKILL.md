---
name: verify-before-accept
description: Do not accept worker output without proof.
---
# verify-before-accept

- Require verify command + pass/fail before final accept.
- On fail: one fix-forward brief with root cause, not shotgun retries.
- Design-only answers need no tests; tree changes always need proof.
