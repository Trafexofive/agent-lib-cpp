# reader (coder-proto)

Task-scoped read-only scout. Returns path evidence for the parent implementer.

```bash
# Usually invoked only as sub-agent of coder-proto
./cortex-mk3 -m manifests/agents/coder-proto/agents/reader/agent.yml --dry-run
```

Tools: `list`, `grep`, `fs_read`, `context_peek`. No write / no exec.
