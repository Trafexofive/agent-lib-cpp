# discovery (coder-proto)

Area mapper for parent implementer. Structure, conventions, risk, open questions.

```bash
./cortex-mk3 -m manifests/agents/coder-proto/agents/discovery/agent.yml --dry-run
```

Tools: list, grep, fs_read, context_peek, exec (RO probes only). Feed: working_directory.
