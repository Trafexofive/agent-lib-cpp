── Agent turn 1 ──
Running verifier to identify failing test.
    <action type="tool" name="exec" id="v1" mode="sync"><{"command":"python3 -m unittest discover -s tests -v"}</action>

── Agent turn 2 ──
Running verifier to identify failing test.
    <action type="tool" name="exec" id="v1" mode="sync"><{"command":"python3 -m unittest discover -s tests -v"}</action>

── Agent turn 3 ──
Running test suite to locate failures.
    <action type="tool" name="exec" id="v1" mode="sync">{"command":"python3 -m unittest discover -s tests -v"}</action>

