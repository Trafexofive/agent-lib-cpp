version: "3"
services:
  build_cache:
    build: .
    ports:
      - "8005:8005"
    volumes:
      - build_cache_data:/data
    environment:
      - SQLITE_DB_PATH=/data/build_cache.db
volumes:
  build_cache_data:
