build/
*.o
*.d
*.a
*.so
cortex-mk3
cortex-mk3-server
cortex-mk3-asan
__pycache__/
.pi/
.keys.env
*.swp
*.swo

# Test binaries (remove from tracking: git rm --cached)
tests/tui/*_test
tests/tui/*_demo
tests/tui/pipe_demo
tests/sandbox/run.sh
tests/scenario-runner
# Runtime artifacts
manifests/_session/

# Temp/garbage files
tmp*.py
full_context.md
iterations.xml
context.jsonl
tests/bench.py

# Experimental/scratch
modules/
docs/architecture/
docs/design/
docs/manifests/

# Test results
tests/results/

# Staged manifests (config/ migration)
staged-manifests/

# POC/archive — graduated patterns only in manifests/
poc/

# Runtime logs
*.log
runs/

# Session handoff / audit artifacts (per-session, never commit)
.artifacts/

# Example compiled binaries (never commit build artifacts)
examples/*_example
examples/c_agent

# Local jsoncpp shared lib (never commit binaries; use system -ljsoncpp)
libjsoncpp.so*
